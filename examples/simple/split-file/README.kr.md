# split-file

## 예제 목적
텍스트 파일의 줄 목록을 제한된 크기의 연속 청크로 나누는 예제입니다.

## 핵심 포인트
- `SplitRequest`가 줄 목록과 청크 크기를 함께 전달하고, 성공 시 중첩 목록으로 각 출력 파일의 줄을 표현합니다.
- 청크 크기는 1~10,000이며, 생성될 청크가 10,000개를 넘으면 `OutputLimitExceeded`를 반환해 출력 예산을 지킵니다.
- Python 구현은 입력 순서를 유지하고 줄을 누락·중복하지 않으며, 빈 목록은 빈 청크 목록으로 성공 처리합니다.
