Generate from Cott contracts; preserve declared types, errors, effects, and invariants.
Iterate/index CottList; type empty CottLists; use pathlib.Path; exhaustively match; never redeclare locals.
cott-domain real.yt_dlp.apply_update return: Call real.yt_dlp.resolve_update_repository through the generated facade and use only Python standard-library HTTPS, hashing, and descriptor-relative filesystem primitives. Stream within the stated bounds; require the official checksum; use no subprocess, shell, package manager, ambient executable, arbitrary repository, unsafe tempfile fallback, unchecked redirect, or check-then-follow path operation. Fail closed when authenticated TLS, no-follow traversal, fsync, or same-directory atomic replacement cannot be guaranteed, and return only the declared sanitized MediaError variants. On CPython POSIX, os.replace supports src_dir_fd/dst_dir_fd even though os.replace itself is absent from os.supports_dir_fd; os.rename is the listed operation for that facility. Check os.rename support and keep actual descriptor-relative os.replace, no-follow traversal and error handling; do not reject a supported platform because its alias is unlisted.
cott-domain real.yt_dlp.build_shortcut_url return: Implement the documented yt-dlp prefixes and URL validation exactly; preserve search text. After handling Search and SearchAll, the remaining ShortcutKind variant is Url. Use the remaining branch directly; do not add a redundant isinstance(ShortcutKind_Url) or unreachable fallback.
cott-domain real.yt_dlp.run return: Preserve the exact Cott Never projection as typing.Never, not typing.NoReturn. Runtime signature comparison distinguishes these annotations even when a type checker treats them as equivalent.
cott-domain real.yt_dlp.write_download_archive return: On CPython POSIX, os.replace supports src_dir_fd/dst_dir_fd even though os.replace itself is absent from os.supports_dir_fd; os.rename is the listed operation for that facility. Check os.rename support and keep actual descriptor-relative os.replace, no-follow traversal, atomicity and error handling; do not reject a supported platform because its alias is unlisted.
