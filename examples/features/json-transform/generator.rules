Python JsonValue values are the cott_runtime classes JsonNull, JsonBoolean, JsonInteger, JsonFloat, JsonString, JsonArray and JsonObject; a JsonObject's `value` is a cott_runtime FrozenMap[str, JsonValue] built as FrozenMap(values={...}). Import them from cott_runtime and never use plain dict, list or str as JSON values.
