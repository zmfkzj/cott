cott-domain declarations.presentation.package_label return: the first tuple element is the plain Label string, so return label.value (the NonEmptyLabel payload), not the NonEmptyLabel newtype itself.
