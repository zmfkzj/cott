In `_cott_impl_SimpleTask_complete`, perform the lifecycle transition by assigning `self.lifecycle = TaskLifecycle_Completed()`, importing `TaskLifecycle_Completed` from `curriculum.trait_protocol_types`.
In `inspect_dyn`, import `TaskView` from the public facade module `curriculum.trait_protocol` (not `curriculum.trait_protocol_types`) and annotate the parameter exactly as `item: Dyn[TaskView[str]]`.
