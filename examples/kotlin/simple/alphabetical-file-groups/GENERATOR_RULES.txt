`cott_runtime.CottList` implements Kotlin's read-only `List`; iterate it directly and construct returned lists with `cott_runtime.CottList(iterable)`. `group_filenames` must call the public facade `curriculum.alphabetical_file_groups.classify_filename` and propagate the first `cott_runtime.Err` unchanged. Classify the first Unicode code point, not the first UTF-16 code unit, and implement full default Unicode case folding for a leading letter. Kotlin/JDK `lowercase()`, `lowercaseChar()`, and locale-sensitive casing are not full Unicode case folding and are not substitutes.
