`cott_runtime.CottList` and `cott_runtime.CottSet` implement Kotlin's read-only collection interfaces; iterate them directly and construct returned lists with `cott_runtime.CottList(iterable)`. Preserve the exact validation priority and deterministic lexicographic ready-step ordering declared by the contract. `plan_pipeline` must call the public facade `curriculum.artifact_pipeline.topologically_order_steps` and propagate its `cott_runtime.Err` unchanged; never import or call another implementation package.
