`cott_runtime.CottList` and `cott_runtime.CottSet` implement Kotlin's read-only collection interfaces; iterate them directly and construct returned lists with `cott_runtime.CottList(iterable)`. Kotlin `String.compareTo` orders UTF-16 code units; where the contract requires Unicode code point order, compare the strings' `codePoints()` sequences.
