Keep the public `foo.bar` facade graph distinct: `process_bar` calls `foo.bar.validate_payload`, then `foo.bar.process_payload_bytes`, then `foo.bar.build_output`, propagating `cott_runtime.Err` results unchanged. `cott_runtime.CottBytes` is the immutable generated bytes value and Kotlin's read-only `List<Byte>`; do not expose or retain a mutable `ByteArray`. All four functions are pure; never invent effects, clients, adapters, retries, caches, or external calls.
