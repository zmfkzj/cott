Use the generated nominal Kotlin values through their public carriers: `step.value` is `Double` and `limit.value` is `UInt`. Return immutable `cott_runtime.CottList` values constructed from an iterable. Compute every candidate exactly as `start + index.toDouble() * step.value`, with a separate binary64 multiplication and addition; do not use repeated accumulation, `Math.fma`, `BigDecimal`, or decimal rounding. Preserve the declared error order, including the termination-only candidate after the output limit.
