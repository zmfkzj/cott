# page-build

## 예제 목적
페이지 원본을 검증하고 출력 경로와 결정적인 HTML로 빌드합니다.

## 핵심 포인트
- 슬러그는 소문자 ASCII 문자·숫자로 된 비어 있지 않은 구간을 단일 하이픈으로 연결해야 하며, 슬러그 오류를 빈 제목보다 먼저 반환합니다. 성공 경로는 `<slug>/index.html`입니다.
- 제목은 `h1`으로, 비어 있지 않은 본문 줄은 입력 순서의 `p`로 렌더링하며 `&`, 꺾쇠, 양쪽 따옴표를 HTML 문자 참조로 이스케이프합니다.
