# publication-workflow

## 예제 목적
게시물 상태와 요청 동작으로 유효한 워크플로 전이를 결정합니다.

## 핵심 포인트
- 전이 표는 초안 제출→검토 중, 검토 중 승인→게시됨, 게시됨 철회→철회됨만 허용하며, 대상 함수는 불가능한 조합에 `Nothing`을 반환합니다.
- 현재 상태가 검토 중이고 요청이 승인인데 편집자 승인이 없으면 `ApprovalRequired`를 반환하고, 그 밖의 잘못된 전이는 `InvalidTransition`으로 처리합니다. 성공 전이는 항상 현재 상태와 다릅니다.
