# process-bar

## 예제 목적
바이트 페이로드를 검증·처리·출력 조립 단계로 나누어 원본 메타데이터를 보존하는 흐름을 보여 줍니다.

## 핵심 포인트
- Cott 계약은 빈 바이트를 `InvalidPayload`로 거부하고, 네트워크 효과를 가진 바이트 처리 단계 뒤에 원본 크기와 형식을 담은 출력을 구성합니다.
- 현재 Python 바인딩의 `process_payload_bytes`는 바이트를 그대로 반환하며, `process_bar`도 빈 입력만 검사한 뒤 입력 바이트와 메타데이터로 출력을 만듭니다.
