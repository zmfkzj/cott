# reputation

## 예제 목적
평판 이벤트를 입력 순서대로 누적하여 음수가 아닌 최종 평판을 계산합니다.

## 핵심 포인트
- 추천·비추천·채택 답변은 각각 `+10`, `-2`, `+15`의 고정 변화량으로 변환됩니다.
- 시작 평판 음수를 먼저 거부하고, 각 이벤트 뒤 `I32` 상한 초과와 음수 전환을 검사해 최초 위반에서 `ReputationOverflow` 또는 `WouldBecomeNegative`를 반환합니다.
