# clip-ranges

## 예제 목적
원본 길이 안의 시간 구간들을 검증하고 총 클립 길이를 계산합니다.

## 핵심 포인트
- 빈 구간 목록을 먼저 거부하고, 각 반열린 구간은 시작이 끝보다 앞서며 끝이 원본 길이를 넘지 않아야 합니다.
- 유효한 구간은 입력 그대로 보존하고 `end_ms - start_ms`를 `U64` 검사 덧셈으로 누적하며, 합계 범위 초과는 `TotalOverflow`로 반환합니다.
