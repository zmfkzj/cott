name: counter_app
description: A Flutter counter backed by a verified Cott Dart module.
publish_to: none
version: 0.1.0+1

environment:
  sdk: ">=3.13.3 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_counter:
    path: cott_module

flutter:
  uses-material-design: true
